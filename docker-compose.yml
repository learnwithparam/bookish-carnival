services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/.venv
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - WATCH=true
      - HEADLESS=true
    command: uv run chainlit run app.py -w --host 0.0.0.0 --port 8000
